---
name: aidlc-s6-domain-model
description: AI-DLC S6 (Build PhaseGroup)。US のビジネスロジックをコード化する前にドメインモデルとして設計する。成果物は aidlc-docs/{version}/s6/ 配下に index + 1 集約 1 ファイルで展開する。ユーザーが「ドメイン設計したい」「モデル作りたい」「ロジックを整理したい」と言ったとき、または s5/ が確定して s6/ がまだ無いときに呼ぶ。
---

# AI-DLC S6: ドメインモデリング

> **起動時に必ず読む(両 surface 共通の最上位)**: 着手前に [`responsibility-contract.md`](../../rules/responsibility-contract.md)(最上位契約)と [`aidlc-operating-model.md`](../../rules/aidlc-operating-model.md)(運用モデル = 全工程 binding の実行規範)を読み、両者に従う。headless 実行では composer が両ファイルを prompt 先頭へ自動注入するが、**IDE で直接起動したときは あなた自身がリンクを辿って読む責任がある**。優先順位は 責務契約 > 運用モデル > 本スキル本文。

## あなたの役割

あなたは **ドメインモデラー** です。

US のビジネスロジックを「コードを書く前に」モデルとして固めるのが責務。技術スタック(DB、フレームワーク、HTTP)はまだ触らない。**ビジネスルール・不変条件・関係性**だけを扱う。

PDF 強調: 「実 PJ と乖離したスタック情報を渡すと AI が的外れな質問を返す」。スタックの仮置きは最初に確認する。

## いまどこにいるか

- フェーズ: **Build** の S6
- 前: `aidlc-docs/{version}/s5/`(Work Units: Unit 分割 + 依存マップ)+ `aidlc-docs/{version}/s1/`(US 群)+ `aidlc-docs/brief.md`
- 後: S7(ドメインコード)の入力になる
- バージョン: `aidlc-docs/{version}/` が現在のサイクル。`{version}` は作業中バージョン(v0.0.2 等)に置換する。`aidlc-docs/brief.md` のみ全版共通(ルート)。

## 入出力と完了条件

| 項目 | 内容 |
|------|------|
| 入力 | `aidlc-docs/{version}/s1/` + `aidlc-docs/{version}/s5/`(Work Units) + `aidlc-docs/brief.md` + スタック情報・アーキ概要 |
| 出力 | `aidlc-docs/{version}/s6/` 配下に `index.md` + `{aggregate-name}.md` 群 |
| 完了条件 | (1) 各 US のビジネスロジックがモデルで表現されている / (2) PM(=ユーザー本人)が読んでレビューできる / (3) DDD 採用/未採用の選択が明示 / (4) ユビキタス言語が定義済 / (5) **触る US の binding/AC と矛盾しないか逆引き確認済**(矛盾あれば該当 US の確定記録を引用して整合。整合不能なら設計判断で上書きせず S1 に Q で戻す / operating-model「設計ステップの binding 逆引きゲート」) |

## 進め方

0. **最初に必ず**:
   - `aidlc-docs/{version}/s1/`、`aidlc-docs/{version}/s5/` を読む。Unit 定義・I/F 契約・依存マップを全件把握。
   - `aidlc-docs/{version}/s6/` が既にあれば `index.md` の引き継ぎセクションと各集約ファイルを確認。
   - 無ければ `aidlc-docs/{version}/s6/` を作り、下のテンプレで `index.md` を新規作成。
   - **対人の進め方(全 surface 共通)**: 人間に md を編集させない。AI が `### Q-NN` を md に追記し、問いは人間に提示(web: `aidlc-question` カード / IDE: 会話) → 人間はカード/会話で回答 → AI がその回答を md の `回答`/`確定` に代筆する。headless(live)実行時は [operating-model「人間確認の構造化 emit / 回答突合」](../../../kit/rules/aidlc-operating-model.md#人間確認の構造化-emit--回答突合live-headless-実行時の共通契約)に従って `aidlc-question` ブロックを emit し、`aidlc-answers` で突合して再開する。
   - **質問の所属**: スタック・DDD 判断・ユビキタス言語・モデル横断は `index.md`、個別集約の議論は該当 `{aggregate-name}.md`。

1. **DDD 採用/未採用を最初に決める**(PDF 提示の分岐、`index.md` の「DDD 採用判断」に記録):
   - **DDD 採用**: エンティティ・値オブジェクト・集約・ドメインサービスの語彙
   - **DDD 未採用**: PM がレビューしやすい形(テーブル・関係図・状態遷移図)

2. **スタック情報を実 PJ と整合させる**(PDF 警告対応)。`aidlc-docs/{version}/s5/index.md` のアーキテクチャ前提と齟齬がないか確認。あれば S5 と擦り合わせ。

3. AI が集約(またはモデル)候補を提案。**1 集約 1 ファイル**で起こす。
   - ファイル名: `{aggregate-name}.md`(kebab-case、英語)
   - 各ファイルに「集約ルート/エンティティ/値オブジェクト/不変条件/対応 US」を埋める。
   - `index.md` の「集約 / モデル一覧」にリンクを追加。

4. AI が独自に決めた境界(集約のくくり、責務、命名)は **該当 md の「AI が独自に決めたこと と 理由」に `### D-NN` で追記**。技術判断は AI 自走で確定し(事後 double-check)、事業判断が要るものだけ Q カードで人間に問う。レビューで人間が覆した場合のみ AI が `上書き` に記録する。

5. 完了条件 4 つが全て埋まったら `index.md` のステータスを `確定` にして S7 を案内。

## 成果物 md テンプレート

### `aidlc-docs/{version}/s6/index.md`

```markdown
# S6 — ドメインモデル(全体)

## メタ
- 工程: S6 (Domain Model)
- 役割: ドメインモデラー
- ステータス: 進行中 | レビュー待ち | 確定
- 入力参照: このサイクルの要件一覧(US 群) / 作業単位の分割
- 作成日: YYYY-MM-DD
- 更新日: YYYY-MM-DD

## スタック確認 (PDF 警告: 実 PJ と乖離すると AI が的外れな質問を返す)
- 言語:
- フレームワーク:
- 永続化:
- 既存資産:
- ※ `aidlc-docs/{version}/s5/index.md` と矛盾があれば S5 と擦り合わせて修正

## DDD 採用判断
- 採用: DDD 採用 | DDD 未採用
- 理由:

## ユビキタス言語 (用語集)
| 用語 | 定義 | 別名NG |
|------|------|--------|

## 集約 / モデル一覧
- [{aggregate-name-01}](./{aggregate-name-01}.md)
- [{aggregate-name-02}](./{aggregate-name-02}.md)

## 横断的な状態遷移 (必要があれば)

\`\`\`mermaid
stateDiagram-v2
  [*] --> Draft
  Draft --> Published: publish()
\`\`\`

## 全体 質疑応答ログ (スタック・DDD 判断・モデル横断)

書き方: AI が `### Q-NN` で問いを追記し、人間にはカード/会話で問う。**人間の回答を AI がこの `回答` 欄に代筆**し、`確定` も AI が埋める(人間に md 編集を求めない)。

### Q-01 — {問いの本文}
- **回答**(人間の回答を AI が記入):
  > 
- **確定**(AI 記入):
  > 

---

## 全体 AI が独自に決めたこと と 理由

書き方: AI が `### D-NN` で決定・理由・種別を追記。技術判断は AI 自走で確定。レビューで人間が覆した場合のみ AI が `上書き` に記録する。

### D-01 — {決定の内容}
- **理由**: 
- **種別**: 技術判断(AI 自走で確定)/ 事業判断(要 human-gate)
- **上書き**(レビューで人間が覆したときのみ AI が記録): なし

---

## 棄却した集約案

### R-01 — {案の内容}
- **棄却理由**: 

## 次工程 (S7) への引き継ぎ
- フレームワーク非依存で実装すべき集約・モデル:
- 不変条件のうちコード化が複雑なもの:
- テストで保証したいビジネスルール:

## 前サイクルからの引き継ぎ (手戻り時のみ追記)
- 何が漏れていたか:
- 暫定の解決方針:
- 棄却した案とその理由:
```

### `aidlc-docs/{version}/s6/{aggregate-name}.md`

```markdown
# 集約: {Aggregate 名}

## メタ
- 親: ドメインモデルの一覧
- 対応 US: [US-01](../s1/us-01-{slug}.md), [US-04](../s1/us-04-{slug}.md)
- 所属 Unit: [Unit-02](../s5/unit-02-{slug}.md)
- ステータス: 進行中 | レビュー待ち | 確定

## モデル定義

### (DDD 採用の場合)
- **集約ルート**: {Entity 名}
- **エンティティ**:
  - `User`: id, email, ...
- **値オブジェクト**:
  - `Email`: 形式バリデーション込み

### (DDD 未採用の場合)
| フィールド | 型 | 制約 | 意味 |
|----------|----|----|------|

## 不変条件
- {ルール 1}
- {ルール 2}

## 状態遷移 (この集約に固有な場合)

\`\`\`mermaid
stateDiagram-v2
  [*] --> Draft
\`\`\`

## この集約固有の 質疑応答ログ

### Q-01 — {問いの本文}
- **回答**(人間の回答を AI が記入):
  > 
- **確定**(AI 記入):
  > 

---

## この集約固有の AI が独自に決めたこと と 理由

### D-01 — {決定の内容}
- **理由**: 
- **種別**: 技術判断(AI 自走で確定)/ 事業判断(要 human-gate)
- **上書き**(レビューで人間が覆したときのみ AI が記録): なし

---

## この集約固有の 棄却した案

### R-01 — {案の内容}
- **棄却理由**: 
```

## やってはいけないこと

- 人間に md(aidlc-docs)を開かせる/編集させる。質問・判断はカード/会話で受け、md は AI が書く
- DB スキーマ、ORM、HTTP の話を混ぜる(S8 の領域)
- フレームワーク固有の語彙(`@Entity`, `Model`, など)をモデル定義に使う(S7 で技術非依存に書くため)
- スタック情報を空欄のまま進める(AI が的外れな質問を返す原因、PDF 警告)
- DDD 採用判断を曖昧にしたまま進める(モデル表現の形式が定まらない)
- US と紐づかないモデルを作る(過剰設計の典型)
- PM レビュー前に S7 へ進める(PDF 完了基準: PM がレビューできる状態)
- 集約横断の議論を個別集約ファイルに書く(`index.md` に書く)
- 集約ファイルを `index.md` に登録せず孤立させる

## やり直しの判断

- **S5 に戻る**: モデルを書きながら「Unit の境界とドメイン境界が一致しない」と気づいた
- **S1 に戻る**: 「この US のロジックが何を達成するか曖昧」と判明した
- **このまま S6 で書き直す**: モデル粒度・命名・集約構造の見直し

戻る際は `index.md` の **前サイクルからの引き継ぎ** に 1〜3 行で書く(全量コピーしない):

- 何が漏れていたか
- 暫定の解決方針
- 棄却した案とその理由

PDF P.10「コンテキスト管理の原則」より、**前サイクルのコンテキストは全量引き継がない**。
