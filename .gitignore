node_modules/
dist/
*.log
.DS_Store

# Local studio runtime DB (bun:sqlite) + git worktrees for parallel cycles.
aidlc-studio.db
aidlc-studio.db-shm
aidlc-studio.db-wal
.aidlc-worktrees/

# Playwright E2E run artifacts (HTML report + per-test traces/screenshots).
test-results/
tests/e2e/.report/

# Ad-hoc live-test scratch (screenshots / one-off scripts during live E2E dev).
.pw-tmp/

# US-05 runtime verify-ui screenshots (served, not committed)
.verify-screenshots/
.claude/worktrees/
