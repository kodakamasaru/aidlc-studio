# Unit-06: フロントエンド共通基盤

## メタ
- 親: [s5/index.md](./index.md)
- 所属 US: [US-09](../s1/us-09-frontend-cleanup.md)(S7-C3)
- Phase: Phase 1(leaf / frontend)
- レイヤ: `web/`(純粋リファクタ)
- ステータス: 確定

## 責務 (1〜2 行)
US-06/US-07 の新規 UI を載せる前に、各画面に重複する **画面ガード(権限チェック)と一覧ソート**を共通フック/ユーティリティへ抽出する純粋リファクタ。機能の振る舞いは変えない(既存 SCR-01〜05 の動作不変)。

## 外部依存
- なし(新規 domain に依存しない)。既存 `web/src/lib/`・`web/src/features/*`・`web/src/components/` のみ対象。
- 利用される側: Unit-07(Step UI)/ Unit-08(リッチ描画)が抽出後の基盤に乗る。

## I/F 定義 (この Unit が公開する契約)

| 公開物 | 形 | 用途 |
|--------|----|------|
| `usePageGuard`(共通フック/コンポーネント) | 画面の権限チェックロジックを 1 箇所に集約 | 各 feature ページがラップして利用 |
| 一覧 Comparator ユーティリティ | ソート関数の共通化(`web/src/lib/`) | 一覧画面(cycles / inbox 等)が共有 |

## 主な AC(US 由来)
- PageGuard 抽象: 各画面の権限チェックが共通フック/コンポーネントに抽出される。
- 一覧 Comparator: ソートロジックが共通ユーティリティに抽出される。
- 既存 SCR-01〜05 の動作不変(リグレッション全 pass)。抽出後の各画面に重複なし。既存テスト全 pass。

## この Unit 固有の 質疑応答ログ

### Q-01 — 既存の重複は具体的にどの画面か(抽出範囲の確定は実装時に grep で確認)
- 提案: `web/src/features/*/{*Page}.tsx` と `web/src/lib/` を実装時に走査し、権限チェックとソートの重複箇所を列挙してから抽出する(現状 features は cycles/inbox/cycle-detail/review)。
- **回答**(ユーザー記入):
  >
- **確定**(AI 記入):
  >

---

## この Unit 固有の AI が独自に決めたこと と 理由

### D-01 — US-06/US-07 の前(Phase 1 / leaf)に実施する
- **理由**: US-09 D-01。新規画面要素を追加する前に共通化すれば新規要素も共通基盤に乗る。後からだと 2 度手間。依存上も leaf なので最初に着手できる。
- **判断**: 承認(2026-06-11 ユーザー一括承認)
- **上書き内容**(上書き時のみ):

---

## この Unit 固有の 棄却した案

### R-01 — v0.0.3 に送る
- **棄却理由**: US-09 R-01。ユーザーが v0.0.2 IN を明示。UI 拡張(Unit-07/08)の前実施で負債増加を防ぐ。
